[phases.install]
cmds = ["npm --prefix nush-agency install", "pip install -r requirements.txt"]

[phases.build]
cmds = ["npm --prefix nush-agency run build"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
