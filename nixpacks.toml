providers = ["python", "node"]

[phases.install]
cmds = ["cd nush-agency && npm install", "pip install -r requirements.txt"]

[phases.build]
cmds = ["cd nush-agency && npm run build"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
