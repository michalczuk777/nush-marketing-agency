{
  "name": "nush-marketing-agency-deploy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix nush-agency install && npm --prefix nush-agency run build",
    "start": "npm --prefix nush-agency run preview -- --host 0.0.0.0 --port $PORT"
  }
}
