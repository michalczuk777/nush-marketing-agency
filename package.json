{
  "name": "nush-marketing-agency-deploy",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "echo 'No build step required for static HTML and FastAPI'"
  }
}
