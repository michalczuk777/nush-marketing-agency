import { motion } from 'motion/react';
import { BarChart3, Workflow, Activity } from 'lucide-react';
const problems = [{ icon: Activity, title: 'Kółka się kręcą, a biznes stoi', desc: 'Kampanie i content działają osobno, ale nic nie łączy ich z wynikiem biznesowym.' }, { icon: Workflow, title: 'Za dużo ręcznej pracy', desc: 'Powtarzalne zadania, ręczne przenoszenie danych i procesy, które powinny być połączone.' }, { icon: BarChart3, title: 'Decyzje na czuja, nie na danych', desc: 'Raporty pokazują ruch i kliknięcia, ale nie odpowiadają, co naprawdę poprawia sprzedaż i marżę.' }];
export default function Problems() { return <section id="problemy" className="py-12 md:py-24 px-6 md:px-10 bg-[#0a0a0a] relative border-b border-white/10"><div className="max-w-7xl mx-auto"><h2 className="font-mono text-xs font-bold uppercase tracking-widest text-neon mb-10">[01] PROBLEMY, KTÓRE ZNAMY</h2><div className="grid md:grid-cols-3 gap-4">{problems.map((prob, i) => <motion.article key={prob.title} initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: i * .1 }} whileHover={{ y: -6, borderColor: 'rgba(0,255,0,.55)' }} className="group p-6 border border-white/10 bg-white/[.02]"><prob.icon className="text-neon mb-12" size={22} aria-hidden="true" /><h3 className="font-bold text-lg uppercase mb-3 text-white">{prob.title}</h3><p className="text-base text-white/55 leading-relaxed group-hover:text-white/75 transition-colors">{prob.desc}</p></motion.article>)}</div></div></section>; }
